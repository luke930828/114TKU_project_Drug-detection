SQL系統
MySQLdrug2026\
-------------------------------------
登錄系統:
super_admin
super_secret_hash
------------------------------------
SERVER:
啟動:python -m uvicorn main:app --reload 
啟動連線 python -m uvicorn main:app --host 0.0.0.0 --port 5000 --reload
	python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
	python -m uvicorn main:app --host 0.0.0.0 --port 8001 --reload
	python -m uvicorn main:app --host 0.0.0.0 --port 8002 --reload
------------------------------------
SQL:
sql內查詢使用者: SELECT * FROM drug_prevention_db.users;
查存的網站 SELECT * FROM suspect_websites;
刪全部:SELECT * FROM drug_prevention_db.suspect_websites;
------------------------------------
TEST WED :
fast API : http://127.0.0.1:8000/docs
------------------------------------
瘦身資料位置:
你的專案資料夾/
│
├── database.py         # 🗄️ 保持原樣：負責資料庫連線與 SQLAlchemy Models[cite: 2]
├── schemas.py          # 📝 新增：統一放置所有 Pydantic Models (如 UserLogin, WebsiteReport)
├── dependencies.py     # 🛡️ 新增：放置共用依賴 (如 get_db, verify_admin, verify_super_admin)
├── utils.py            # 🛠️ 新增：放置共用邏輯 (如計算分數的 calculate_multimodal_risk, dispatch_to_ai_engines)[cite: 3]
├── main.py             # ⚡ 瘦身後的系統進入點 (只剩下幾十行)
│
└── routers/            # 🚀 存放各個功能模組的資料夾
    ├── __init__.py     
    ├── auth.py         # 對應模組一：管理員登入[cite: 3]
    ├── scan.py         # 對應模組二：輸入網址即時識別[cite: 3]
    ├── crawler.py      # 對應模組三、四、十：爬蟲報表接收與列表查詢[cite: 3]
    ├── whitelist.py    # 對應模組六：白名單維護[cite: 3]
    ├── ai_engine.py    # 對應模組七、八：YOLO 與 NLP 結果接收通道[cite: 3]
    └── export.py       # 對應模組九：報表匯出 (Excel)[cite: 3]