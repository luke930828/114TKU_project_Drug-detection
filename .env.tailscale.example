# 跨機版：跟 .env.local 唯一的差別就是位址跟 timeout。程式碼一行都不用改。
#
# 建議在 Tailscale 後台打開 MagicDNS，然後用主機名而不是 100.x.x.x：
# IP 換了（重裝、換帳號、ephemeral node）名字不會變，寫死 IP 會像現在一樣散落各處要改。

# ---- 資料庫 ----
DB_USER=root
DB_PASSWORD=
DB_NAME=drug_prevention_db
# 資料庫如果不在本機，填 tailnet 主機名
DB_HOST=node-backend.你的tailnet.ts.net

# ---- 模組位址：改成各同學機器的 MagicDNS 名稱 ----
# 對照現在寫死在程式裡的 IP：
#   NLP     100.69.185.94:8000
#   YOLO    100.101.167.105:5000
#   爬蟲    100.122.162.47:8001
#   前端    100.123.184.43:8002
NLP_BASE_URL=http://node-nlp.你的tailnet.ts.net:8000
YOLO_BASE_URL=http://node-yolo.你的tailnet.ts.net:5000
CRAWLER_BASE_URL=http://node-crawler.你的tailnet.ts.net:8001

# ---- 網路行為：跨機 RTT 高，一定要放寬 ----
# 同機 loopback ~0.1ms；Tailscale 直連 5-30ms；走 DERP relay 可能破 100ms。
# 原本在單機測得好好的 timeout=1 上 tailnet 就會開始隨機失敗。
HTTP_TIMEOUT=30
HTTP_RETRIES=4

INTERNAL_API_TOKEN=

VITE_API_BASE_URL=http://node-backend.你的tailnet.ts.net:8000
CORS_ORIGINS=http://node-frontend.你的tailnet.ts.net:8080

# ---- NLP 微調模型（Hugging Face Hub repo id，公開 repo 不用改）----
MODEL_ID=matt0513/drug-detection-xlm-roberta
